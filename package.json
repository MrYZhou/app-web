{
  "name": "app-larry",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "element-plus": "^2.2.6", // ui库
    "vue": "^3.2.25",
    "vuedraggable": "^2.24.3" //拖拽库
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.3", // vue3单文件支持
    "typescript": "^4.5.4",
    "unplugin-auto-import": "^0.9.2", //自动导入api
    "unplugin-vue-components": "^0.20.1", //自动导入组件
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7" // ts转js
  }
}